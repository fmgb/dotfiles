[user]
	email = fmgb92@gmail.com
	name = fmgb
[github]
	user = fmgb
	email = fmgb92@gmail.com

[core]
    editor = 'emacsclient -c' --wait

[color]
   	ui = true

[push]
	default = simple

[alias]
	st = status -sb
	ci = commit
	co = checkout
	status = status -sb

[commit]
	template = ~/.gitmessage

[status]
	# Painfully verbose (but vitally important) info about submodules.
    	submodulesummary = true

[web]
  	browser = firefox

[color "branch"]
	current = cyan bold
	local = green bold
	remote = red bold
	upstream = red bold
	plain = yellow reverse

[color "status"]
    	header = white
   	added = green
  	updated = blue
 	changed = yellow
	unracked = red

